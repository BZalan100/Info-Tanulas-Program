#include <stdio.h>
#include <string.h>
#include <iostream>
using namespace std;
int main()
{
    char x[99];
    cout << "szoveg: ";
    fgets(x, sizeof(x), stdin);
    int i;
    for (i = 0; i < strlen(x)-1; i += 3)
    {
        cout << x[i];
    }
    return 0;
}
